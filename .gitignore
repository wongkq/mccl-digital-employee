# 私有参考资料，永不入库
测试.md

# 环境配置含内网IP与主机映射，只提交 .example 模板
mccl-env.json
mccl-env.sh

# 运行产物（编译/测试日志，体积大且含内网信息）
.mccl-runs/

# bench 队列状态与调度日志（本地运行产物）
.mccl-bench-queue/

# gpu_health_check 远程拉取的结果目录（临时产物）
gpu_health_results_*/

# SDD执行账本（本地scratch）
.superpowers/
