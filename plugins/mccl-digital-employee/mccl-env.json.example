{
  "_comments": {
    "usage": "cp 此文件为 mccl-env.json 并填入真实值。mccl-env.json 不入库（见 .gitignore）。所有 agent 经 bin/mccl-env-load.py 加载（eval \"$(python3 $TOOLKIT_ROOT/bin/mccl-env-load.py)\"），不得硬编码。loader 自动跳过 _ 前缀键（如本 _comments）。",
    "cross_platform": "配置脱离 bash 语法、OS 无关：Windows/macOS/Linux 用同一份 JSON。loader 是跨平台 Python，emit 的 bash export 由 agent 在本地 bash 里 eval（Windows 走 WSL）。loader 只在本地跑，远端机器不需要 python3。",
    "raw_vs_derived": "本文件只填 raw 键（14 个）。派生量（MCCL_NODE0_IP/MCCL_NNODES/MCCL_NP/MCCL_HOST_SPEC/MCCL_MACA_LIB_DIR/MCCL_REMOTE_SRC/MCCL_LD_LIBRARY_PATH）由 loader 从 raw 算，不写在这里——改一处 IP，派生量自动跟上。",
    "setup_ssh": "换机器/换IP时：改完下面的IP，跑一次 bash <插件>/bin/mccl-setup-ssh。它只需配 本机 -> NODE0 一条链路（工具包一律经NODE0跳转），并检查 NODE0 -> 其余节点 和容器是否可达。密码只在交互时输入一次，不存盘。",
    "topology": "只支持三档节点数：1个IP->单节点冒烟（容器内 --mca plm isolated，不测跨节点对称内存）；4个IP->OAM32（32卡）；8个IP->OAM64（-np 64）。其余节点数 MCCL 只硬编码 OAM32/OAM64，CliqueManager::IsSupported() 不匹配，对称内存静默fallback到Ring/Tree，agent 遇到其余节点数会停止并上报。",
    "gpus_per_node": "MCCL_GPUS_PER_NODE 必须=8 才能对上代码硬编码的 nodeSize=8/GROUP=8（PCIe Switch 硬件决定）。填其他值：单节点能跑基础 AllReduce 冒烟，但对称内存/FC kernel 路径不启用，agent 会在报告声明未覆盖；多节点(4/8)配非8卡会被判为不支持的拓扑，停止上报。",
    "maca_path": "MCCL_MACA_PATH 必须指向含正确 mcMemFabricHandle_t 定义（1112字节，含scatter buffer）的 MACA 安装。厂商默认 MCCL_VENDOR_MACA_PATH（通常 /opt/maca）里的是80字节旧stub，不能拿来编译，否则跨节点对称内存句柄异常/UDS refused。详见 references/mccl-build-pitfalls.md。",
    "vendor_maca": "MCCL_VENDOR_MACA_PATH 是厂商标准 MACA（MetaX SDK 默认安装）。其 mcMemFabricHandle_t 是旧stub不能编译；但全量重编时先从它派生编译器与 cu-bridge 工具链路径、以及 mxgpu_llvm/bin 符号链接源。详见 mccl-build-pitfalls.md 第3、4条。",
    "maca_lib_dir": "MCCL_MACA_LIB_DIR（loader 派生 = MCCL_MACA_PATH/lib）：mpirun 实际加载 libmccl.so 的库目录，也是跨节点分发的落点。必须与 MCCL_LD_LIBRARY_PATH 的库目录部分是同一个目录。",
    "workdir": "MCCL_REMOTE_WORKDIR：agent 在远程的可写范围边界，此目录之外一律禁止修改。不要带尾斜杠。",
    "remote_src": "MCCL_REMOTE_SRC（loader 派生 = MCCL_REMOTE_WORKDIR/MCCL_REMOTE_SRC_REPO_NAME）：编译节点上的源码目录。换环境通常只改 MCCL_REMOTE_SRC_REPO_NAME，WORKDIR 变了自动跟上。",
    "mpi_run": "MCCL_MPIRUN：mpirun 可执行文件路径（多节点直接调用 $MCCL_MPIRUN）。单节点用裸 mpirun（需该目录在容器 PATH）。注意是可执行文件，不是 bin 目录。",
    "perf_bins": "MCCL_PERF_BIN_SYM（对称内存 perf 二进制，单节点/场景B用，场景B多节点带 -R 2）；MCCL_PERF_BIN_ASYM（非对称 perf 二进制，场景A用，不带 -R）。单节点只有 SYM 被调用。路径须为容器内可执行路径。",
    "ld_library_path": "MCCL_LD_LIBRARY_PATH（loader 派生 = MCCL_MACA_LIB_DIR:MCCL_OMPI_LIB_PATH）：多节点 mpirun 经 -x 注入。MCCL_OMPI_LIB_PATH 是 libmpi.so 所在目录。",
    "tcp_if": "MCCL_TCP_IF_INCLUDE：MPI TCP 网段 CIDR（多节点 btl_tcp_if_include 用；单节点 --mca plm isolated 不用）。",
    "ssh_opts": "MCCL_SSH_OPTS：所有 ssh/scp 必须带这组选项。BatchMode=yes 让密钥没配好时立刻失败而不是弹密码提示卡死。一般不需改。"
  },
  "MCCL_NODES": "<node0-ip> <node1-ip> <node2-ip> <node3-ip>",
  "MCCL_GPUS_PER_NODE": 8,
  "MCCL_SSH_OPTS": "-o BatchMode=yes -o ConnectTimeout=10 -o StrictHostKeyChecking=accept-new",
  "MCCL_CONTAINER": "<container-name>",
  "MCCL_MACA_PATH": "<maca-path>",
  "MCCL_VENDOR_MACA_PATH": "<vendor-maca-path>",
  "MCCL_REMOTE_WORKDIR": "<remote-workdir>",
  "MCCL_LOCAL_SRC": "<local-repo-path>",
  "MCCL_REMOTE_SRC_REPO_NAME": "<repo-dir-name>",
  "MCCL_MPIRUN": "<mpirun-path>",
  "MCCL_PERF_BIN_ASYM": "<asymmetric-perf-binary>",
  "MCCL_PERF_BIN_SYM": "<symmetric-perf-binary>",
  "MCCL_OMPI_LIB_PATH": "<ompi-lib-path>",
  "MCCL_TCP_IF_INCLUDE": "<cidr>"
}
